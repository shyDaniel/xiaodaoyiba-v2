// @xdyb/client — bootstrap stub.
//
// React + PixiJS bring-up lands in subsequent iterations (Landing / Lobby /
// Game pages, GameStage canvas, BattleLog rail). This file exists so the
// Vite entry resolves and the package boundary is real for the workspace.

import { SHARED_PACKAGE_VERSION } from '@xdyb/shared';

// eslint-disable-next-line no-console
console.log(`[xdyb-client] bootstrap stub — shared@${SHARED_PACKAGE_VERSION}`);

export {};
